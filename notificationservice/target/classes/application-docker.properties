# Docker profile configuration for notification service

# Kafka configuration for Docker environment
spring.kafka.bootstrap-servers=kafka:9092

# Database configuration is already set in docker-compose.yml environment variables
# These settings will override the defaults in application.properties